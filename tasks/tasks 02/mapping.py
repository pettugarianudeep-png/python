#  Use map() to convert the list of names into uppercase. 

# names = ["anudeep","sandeep","harsha"]
# upper_case = list(map(lambda x: x.upper(),names))

# print(upper_case)

#use map() to calculate the square root of numbers in a list.

# nums = (4,9,16,25)

# result = list(map(lambda x: x**0.5,nums))

# print(result)


#Given prices list, use map() to add 18% GST to each value.

# price = (100,200,300)

# #gst = price+(18/100*price) formula

# result = list(map(lambda x: x*0.18,price)) # 0.18 means 18/100
# print(result)

# Use map() to extract the first character from each word in the list. 

# words = ["apple","banana","cherry"]

# result = list(map(lambda x : x[0],words))
# print(result)

#Convert list of temperatures from Fahrenheit to Celsius using map() 
temperatures = [35,65,75]

# celsius = list(map(lambda x: (x-32)*5/9,temperatures))
# print(celsius)
